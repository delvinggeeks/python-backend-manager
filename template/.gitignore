# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
dist/
build/

# Env / secrets
.env
.env.*
!.env.example
# Inert in a generated service; it matters in the template repo, where this same file governs
# `template/` and `.env.*` above would otherwise ignore the un-rendered source of the line above.
!.env.example.jinja

# Local scratch (Schemathesis fuzz runner: throwaway DB + server log)
.fuzz*

# OS / editor
.DS_Store
.idea/
.vscode/
