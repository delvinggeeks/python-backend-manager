# CLAUDE.md — {{ project_name }}

@AGENTS.md

A uv-managed FastAPI backend (agent framework: **{{ agent_framework }}**), generated from
python_backend_manager. The conventions and decision rules live in `AGENTS.md` (imported
above). This file adds only the Claude-Code-specific surface — which commands and
subagents exist. No rules are duplicated here; if something here ever disagrees with
`AGENTS.md`, `AGENTS.md` wins.

## Commands
- `/check` — run all gates (ruff + mypy + pytest) and report pass/fail per gate.
- `/feature "<what to build>"` — implement a feature end-to-end, then self-review.

## Subagents (read-only)
- `code-reviewer` — review a diff for correctness, async pitfalls, and convention breaks.
- `build-judge` — PASS/FAIL a feature against its acceptance criteria + a next prompt.
